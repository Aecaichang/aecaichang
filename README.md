# aecaichang Blog

บล็อกส่วนตัวที่สร้างด้วย **Astro** เพื่อประสิทธิภาพสูงสุด รองรับการเขียนเนื้อหาผ่าน
**Markdown/MDX** และจัดการเนื้อหาผ่าน **Decap CMS** ได้โดยตรงจากหน้าเว็บ

## ✨ คุณสมบัติเด่น

- **Ultra-fast Performance**: สร้างด้วย Astro Static Site Generation (SSG)
  โหลดเร็วมาก
- **SEO Friendly**: มี Meta Tags, Open Graph for Social Share, Sitemap, และ
  Canonical URLs พร้อมใช้งานทันที
- **Content Management**:
  - เขียนผ่านไฟล์ Markdown/MDX ในโปรเจกต์โดยตรง
  - หรือใช้ **Decap CMS** (git-based) เพื่อจัดการเนื้อหาผ่านหน้า Admin UI ที่สวยงาม
- **Image Optimization**: ระบบจัดการรูปภาพอัตโนมัติ (Resize, Format conversions)
- **RSS Feed**: รองรับ RSS เต็มรูปแบบ
- **Responsive Design**: รองรับการแสดงผลทุกหน้าจอ

---

## 🚀 เริ่มต้นใช้งาน (Local Development)

1. ติดตั้ง Dependencies:
   ```bash
   npm install
   ```

2. รันเซิร์ฟเวอร์จำลอง:
   ```bash
   npm run dev
   ```

3. สร้างบทความใหม่ (ผ่าน command line):
   ```bash
   npm run new:post -- "ชื่อบทความของคุณ"
   ```

---

## 📝 การจัดการเนื้อหา (Content)

### วิธีที่ 1: ผ่าน Decap CMS (แนะนำสำหรับรูปภาพและเขียนบทความ)

เข้าไปที่ `https://your-domain.com/admin` (เช่น
`https://aecaichang.vercel.app/admin`) แล้วล็อกอินด้วย GitHub

- **ข้อดี**: อัพโหลดรูปภาพง่าย, มี UI จัดการ, ไม่ต้องยุ่งกับโค้ด
- **รูปภาพ**: เมื่ออัพโหลดผ่าน CMS ระบบจะเก็บไว้ที่ `src/assets/blog/` โดยอัตโนมัติ และใช้
  path แบบ alias (`@assets/blog/...`) เพื่อความสะดวกและรองรับการ Optimize

### วิธีที่ 2: เขียนผ่าน VS Code (Markdown/MDX)

ไฟล์บทความจะอยู่ที่ `src/content/blog/`

**โครงสร้าง Frontmatter:**

```yaml
---
title: "ชื่อบทความ"
description: "คำโปรยสั้นๆ สำหรับ SEO และหน้าแรก"
pubDate: 2026-02-07
author: "ชื่อผู้เขียน"
heroImage: "@assets/blog/my-cover-image.jpg" # ใช้ path แบบนี้ได้เลย
tags: ["tag1", "tag2"]
gallery: # (Optional) สำหรับแสดงคลังภาพท้ายบทความ
  - image: "@assets/blog/photo-1.jpg"
    alt: "คำอธิบายภาพ"
    caption: "ใต้ภาพ"
---
```

**การใส่รูปในเนื้อหา (Body):** สามารถใช้ Markdown Syntax ปกติได้เลย:

```markdown
![คำอธิบายภาพ](@assets/blog/image-name.jpg)
```

หรือใช้ HTML `<img>` ก็ได้หากต้องการปรับแต่งเพิ่มเติม

---

## 🖼️ ระบบรูปภาพ (Image System)

โปรเจกต์นี้ตั้งค่า alias `@assets` ให้ชี้ไปที่ `src/assets`
เพื่อความสะดวกในการอ้างอิงไฟล์และรองรับ Image Optimization ของ Astro

- **ที่เก็บรูป**: `src/assets/blog/`
- **การเรียกใช้**: `@assets/blog/ชื่อไฟล์.jpg`

---

## ⚙️ การตั้งค่า Decap CMS (Vercel + GitHub)

เพื่อให้หน้า Admin ใช้งานได้และรูปภาพแสดงผลถูกต้อง:

1. **GitHub OAuth App**:
   - สร้าง OAuth App ใน GitHub Developer Settings
   - Homepage URL: `https://aecaichang.vercel.app`
   - Authorization callback URL: `https://aecaichang.vercel.app/api/callback`

2. **Vercel Environment Variables**:
   - `GITHUB_OAUTH_CLIENT_ID`: (Client ID จากข้อ 1)
   - `GITHUB_OAUTH_CLIENT_SECRET`: (Client Secret จากข้อ 1)

3. **Deploy**:
   - เมื่อตั้งค่าเสร็จแล้ว ให้ Redeploy ใน Vercel เพื่อให้ค่า Environment Variables มีผล

---

## 🔍 SEO & Meta Tags

เว็บนี้รองรับ SEO เต็มรูปแบบ:

- **Title & Description**: ดึงจาก frontmatter ของแต่ละโพสต์
- **Open Graph (Facebook/LinkedIn)**: แสดงรูปปกบทความ (heroImage) อัตโนมัติเมื่อแชร์ลิงก์
- **Twitter Card**: แสดงผลแบบ Large Image
- **Sitemap**: สร้างไฟล์ `sitemap-index.xml` และ `sitemap-0.xml` อัตโนมัติที่ build
  time
- **Robot.txt**: อนุญาตให้ Bot เก็บข้อมูลได้ตามปกติ

---

## 🛠️ คำสั่งต่างๆ

- `npm run dev`: รันเซิร์ฟเวอร์ (localhost:4321)
- `npm run build`: สร้างไฟล์สำหรับ Production (dist/)
- `npm run preview`: ดูผลลัพธ์ของไฟล์ที่ build แล้ว
- `npm run studio`: (ถ้าใช้ Sanity - ปัจจุบันเปลี่ยนมาใช้ Decap CMS แล้ว
  แต่คำสั่งยังอยู่เผื่ออนาคต)
